import {FoodState} from './foodState';

const INITIAL_FOOD_STATE: FoodState = {
  title: '',
  product: null,
  isProductSaving: false,
  dish: null,
  isDishSaving: false,
};

export default INITIAL_FOOD_STATE;
